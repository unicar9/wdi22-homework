// <!DOCTYPE html>
// <html><head>
// <meta charset=utf-8 />
// <title>Return first and last name from a form - w3resource</title>
// </head><body>
// <form id="form1" onsubmit="getFormvalue()">

// First name: <input type="text" name="fname" value="David"><br>
// Last name: <input type="text" name="lname" value="Beckham"><br>

// <input type="submit" value="Submit">
// </form>
// </body>
// </html>

What are you doing for this exercise? - looping through the first and the last name

$("form1"){

  var fullName = document.get

  if(fullName.Elements = "")
})

function (formValue){

  //define what you want the fullName to loop through
  var fullName = document.getElementById("form1");


  //now run the actual loop we are going to use.
    for (var i = 0; i fullName.length; i++) {

      if (name.Elements[i].value! = "Submit") {

        console.log(fullName.Elements[i].value);
      }
    }
});


function (){
  var volume = document.querySelector('div radius');

  volume(parseInt(cn,10).innerHtml)= content

}


function volume_sphere {

  var volume;
  var radius = document.getElementbyId('radius').value;

  radius = Math.abs(radius);

  volume = (4/3)*Math.PI* Math.pow(radius, 3);
  volume = volume.toFixed(4);

  documentget the //id

  return: false

}

function image {

  var images = document.querySelectorAll ('img');

  //need to include the variable to push them into
  // needed to loop through them to pull them out
  //



  selection = math.random(math.floor (images))

  return selection;

}

var preBuffera

//steps

//create the random image number

function getRandomInt (min, max) {

  for loop

}

create random image number

function

0 the first image

var newImage = getRandomInt(0, preBuffer -1)

remove the previous mage
display the image - document.body.appendChild(newIMage)


// write a program to highlight bold words of the following paragraph

function higlight (){

  search through the lines to find the bold text

  for (var i = 0; i < array.length; i++) {

    // get the elements that we are going to be bold
    var bold = getElementsbybold

    if (bold = css bold selector) {
      highlight the text
    }

    return and get out of the function;

  }


//steps

//create list of all the bold items

var boldItems;
window.onLoad = getBold_items ();

collect the tags

function (){
  bold_items = document.getElementsbyTagName ('stong');
}

// iterate over all teh bold tags and change colour

for (var i = 0; i < array.length; i++) {
  bold_items[i]style.color = 'green'; // now you have made it change colour
}


// enter the mouse function

function return (){

  for (var i = 0; i < bold_Item.length; i++) {
   b  bold_items[i]style.color = 'green';
  }

}

// add list element to ul list

function (){
  $("#header ul").append('select the ara')
}

//add list and remove

$( "input" )
  .keyup(function() {
    var tvalue = $( this ).val();
    console.log(tvalue);
  })
.keyup();

// just get to do a functiion for the whole thing

function (){
  var removecss = $( this ).removeClasses();
  console.log(removeCss);
})


//check to see if a jquary is a Objectives

function (){
  var check = $("object select").object();

  if (check object)
  return
}

//left and right mouse click
$(document).on("contextmenu", ".element", function (e)){
  console.log("Right Mouse Clicked");
  return false;
});

$(document).on('click', ".element", function (e)func

function (e) this is what is going to be passed from event to event
